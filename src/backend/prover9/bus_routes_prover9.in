set(production).

formulas(assumptions).
start(a).
goal(f).
stop(a). stop(b). stop(c). stop(d). stop(e). stop(f).
connects(a,b,l1).
connects(b,c,l1).
connects(c,d,l2).
connects(d,f,l2).
end_of_list.

formulas(productions).
start(X) -> visited(X).
visited(X) & connects(X,Y,L) & ~visited(Y) -> visited(Y), take(X,Y,L).
end_of_list.